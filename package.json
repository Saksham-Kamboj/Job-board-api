{
  "name": "job-board-api",
  "version": "1.0.0",
  "description": "Mock API using json-server",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "start:render": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
